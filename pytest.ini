[pytest]
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts =
    -ra
    # --cov=weighrand --cov-branch  # Skip coverage by default due to significant test time increase.
